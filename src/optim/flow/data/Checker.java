
public class Checker {
    // Todo: Complete
    /**
     * Checks if a solution is realisable.
     * 
     * @param instance The network flow problem.
     * @param solution The solution to be checked relative to the instance param.
     * 
     * @return boolean specifying the solution's realisability.
     */
    public boolean check(Instance instance, Solution solution) {
        boolean isSolutionValid = false;

        return isSolutionValid;
    }
}
